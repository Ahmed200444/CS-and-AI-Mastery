(()=>{'use strict';const STATUS_URL='/api/github/status',FILE_URL='/api/github/file',STORE_KEY='csai-github-preferred-repo';let statusPromise=null;const clean=v=>String(v||'').trim(),slug=v=>clean(v).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'item';function courseId(){return slug(document.body.dataset.courseId||document.documentElement.dataset.courseId||location.pathname.split('/').pop()?.replace(/\.html$/,'')||document.querySelector('h1')?.textContent||'course')}function ext(code,hint=''){hint=hint.toLowerCase();if(/c\+\+|cpp/.test(hint)||/#include\s*</.test(code))return'cpp';if(/python|\bpy\b/.test(hint)||/^\s*(def |from |import )/m.test(code))return'py';if(/sql/.test(hint)||/\b(select|insert|update|create table)\b/i.test(code))return'sql';if(/html/.test(hint)||/<[a-z][\s\S]*>/i.test(code))return'html';if(/javascript|\bjs\b/.test(hint)||/\b(const|let|function)\b|=>/.test(code))return'js';return'txt'}function containerFor(button){return button.closest('[data-project-card],[data-exercise],[data-practice],.exercise,.practice,.csai-example-card,.csai-example,.project-card,.project-workspace,.evergreen-example,section,article')||button.parentElement}function details(button){const root=containerFor(button),example=!!button.closest('.csai-example,.csai-example-card,.evergreen-example')||button.dataset.finalKind==='example',project=button.dataset.finalKind==='project'||button.hasAttribute('data-project-publish')||!!button.closest('[data-project-card],.project-card,.project-workspace'),kind=project?'projects':example?'examples':'practice',editor=root?.querySelector('textarea[data-project-editor],textarea[data-editor],textarea[data-dual-editor],textarea'),pre=root?.querySelector('pre code,pre'),code=clean(editor?.value??pre?.textContent),title=clean(root?.dataset.title||root?.querySelector('h2,h3,h4,.title,.exercise-title,.project-title')?.textContent||button.dataset.title||(example?'Example':project?'Project':'Practice')),hint=clean(button.dataset.dualPublishLanguage||button.dataset.language||editor?.dataset.language||root?.dataset.language||pre?.className),extension=ext(code,hint),item=slug(title),filename=kind==='examples'?`example.${extension}`:`${item}.${extension}`,base=`student-code/${kind}/${courseId()}/${item}`,description=clean(root?.querySelector('.description,.project-description,.exercise-description,p')?.textContent);return{root,kind,code,title,extension,base,codePath:`${base}/${filename}`,readmePath:`${base}/README.md`,description}}async function status(){if(!statusPromise)statusPromise=fetch(STATUS_URL,{credentials:'same-origin'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'GitHub status failed');return d}).catch(e=>{statusPromise=null;throw e});return statusPromise}function selectedRepo(s){const preferred=localStorage.getItem(STORE_KEY);return s.repositories?.find(r=>r.full_name===preferred)?.full_name||s.repositories?.find(r=>r.full_name==='Ahmed200444/CS-and-AI-Mastery')?.full_name||s.repositories?.[0]?.full_name||''}function runLine(ext,path){if(ext==='py')return`python ${path.split('/').pop()}`;if(ext==='js')return`node ${path.split('/').pop()}`;if(ext==='cpp')return`g++ ${path.split('/').pop()} -std=c++17 -o app && ./app`;if(ext==='html')return'Open the HTML file in a browser.';if(ext==='sql')return'Run the SQL in your preferred SQL database or the CS & AI Mastery SQL runner.';return'Open the file in the appropriate tool for its language.'}function readme(d){return`# ${d.title}\n\n**Course:** ${courseId()}  \n**Type:** ${d.kind==='projects'?'Project':d.kind==='examples'?'Example':'Practice / Exercise'}  \n**Language:** ${d.extension.toUpperCase()}\n\n## Objective\n\n${d.description||`Practice and demonstrate the concepts used in ${d.title}.`}\n\n## Code\n\nMain file: \`${d.codePath.split('/').pop()}\`\n\n## How to run\n\n\`${runLine(d.extension,d.codePath)}\`\n\n## Learning note\n\nCompleted as part of CS & AI Mastery. This README is tied to this individual item so the repository stays readable when lessons are revisited.\n`}function setMessage(button,text,kind=''){let row=button.closest('.csai-final-publish-row')||button.parentElement,el=row?.querySelector('.csai-final-publish-status');if(!el&&row){el=document.createElement('span');el.className='csai-final-publish-status';row.appendChild(el)}if(el){el.textContent=text;el.className=`csai-final-publish-status ${kind}`}}async function publish(button,isReadme){const d=details(button);if(!d.code){setMessage(button,'Add code first','is-error');return}setMessage(button,isReadme?'Adding README…':'Publishing…');try{const s=await status();if(!s.connected)throw new Error('Connect GitHub first.');const repository=selectedRepo(s);if(!repository)throw new Error('Choose a GitHub repository first.');const path=isReadme?d.readmePath:d.codePath,content=isReadme?readme(d):d.code,response=await fetch(FILE_URL,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSAI-CSRF':s.csrf},body:JSON.stringify({repository,path,content,createOnly:true,requirePath:isReadme?d.codePath:undefined,message:isReadme?`Add README for ${d.title}`:`Add ${d.title} from CS & AI Mastery`})}),data=await response.json();if(!response.ok)throw new Error(data.error||'GitHub publish failed');setMessage(button,data.alreadyExists?(isReadme?'README already added ✓':'Already published ✓'):(isReadme?'README added ✓':'Published ✓'),'is-ok')}catch(error){setMessage(button,error.message,'is-error')}}function pair(existing,kind){if(existing.dataset.finalPublishReady)return;existing.dataset.finalPublishReady='1';existing.dataset.finalKind=kind;existing.setAttribute('data-final-publish','');existing.removeAttribute('data-publish');existing.removeAttribute('data-project-publish');let row=existing.closest('.csai-final-publish-row');if(!row){row=document.createElement('span');row.className='csai-final-publish-row';existing.parentNode?.insertBefore(row,existing);row.appendChild(existing)}if(!row.querySelector('[data-final-readme]')){const readme=document.createElement('button');readme.type='button';readme.textContent='Add a README';readme.setAttribute('data-final-readme','');readme.dataset.finalKind=kind;row.appendChild(readme)}if(!row.querySelector('.csai-final-publish-status')){const s=document.createElement('span');s.className='csai-final-publish-status';s.setAttribute('aria-live','polite');row.appendChild(s)}}function enhance(){document.querySelectorAll('[data-publish],[data-dual-publish-language]').forEach(b=>pair(b,'practice'));document.querySelectorAll('[data-project-publish]').forEach(b=>pair(b,'project'));document.querySelectorAll('.csai-example-actions').forEach(actions=>{if(actions.querySelector('[data-final-publish]'))return;const publish=document.createElement('button');publish.type='button';publish.textContent='Publish to GitHub';publish.setAttribute('data-final-publish','');publish.dataset.finalKind='example';actions.appendChild(publish);pair(publish,'example')})}document.addEventListener('click',event=>{const button=event.target.closest?.('[data-final-publish],[data-final-readme]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();publish(button,button.hasAttribute('data-final-readme'))},true);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();new MutationObserver(()=>enhance()).observe(document.documentElement,{childList:true,subtree:true})})();
