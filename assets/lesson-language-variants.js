(function(){
'use strict';
var MODE_PREFIX='csai-course-language-v2:';
var FLEXIBLE_IDS=['dsa','problem-solving','oop','algorithms','data-structures'];
var timer=0;
function meta(){try{var n=document.getElementById('course-page-meta');return n?JSON.parse(n.textContent||'{}'):{};}catch(e){return{};}}
function courseId(){var m=meta();return String(m.id||location.pathname.split('/').pop()||'').replace(/\.html$/,'').toLowerCase();}
function title(){var h=document.querySelector('.hero h1'),m=meta();return String(m.title||(h&&h.textContent)||'').toLowerCase();}
function flexible(){var id=courseId(),t=title(),all=id+' '+t;if(/python|javascript|typescript|sql|html|css|react|node|java\b|c\+\+/.test(all))return false;return FLEXIBLE_IDS.some(function(x){return id===x||id.indexOf(x)>=0;})||/data structures|algorithms|problem solving|object[ -]oriented/.test(t);}
function mode(){try{var m=localStorage.getItem(MODE_PREFIX+courseId());return m==='cpp'||m==='dual'||m==='python'?m:'python';}catch(e){return'python';}}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function looksCpp(code){code=String(code||'');return /#include\s*[<"]|\busing\s+namespace\s+std|\bstd::|\bcout\s*<<|\bcin\s*>>|\bvector\s*</.test(code)||/\bint\s+main\s*\(/.test(code);}
function looksPython(code){code=String(code||'');return /(^|\n)\s*(def\s+|class\s+\w+.*:|from\s+|import\s+|for\s+\w+\s+in\s+|while\s+.+:|if\s+.+:|elif\s+.+:|print\s*\()/.test(code)||/\brange\s*\(|\blen\s*\(|\.append\s*\(/.test(code);}
function topicText(lesson){var h=lesson.querySelector('summary .title,summary,h3'),s=h?h.textContent:'';return (s+' '+String(lesson.getAttribute('data-lesson')||'')).toLowerCase();}
function pyProgram(topic){
 if(/linked/.test(topic))return "class Node:\n    def __init__(self, value):\n        self.value = value\n        self.next = None\n\nhead = Node(10)\nhead.next = Node(20)\nhead.next.next = Node(30)\n\ncurrent = head\nwhile current:\n    print(current.value)\n    current = current.next";
 if(/stack/.test(topic))return "stack = []\nstack.append(10)\nstack.append(20)\nstack.append(30)\n\nprint(stack[-1])\nstack.pop()\nprint(stack)";
 if(/queue/.test(topic))return "from collections import deque\n\nqueue = deque()\nqueue.append(10)\nqueue.append(20)\nqueue.append(30)\n\nprint(queue[0])\nqueue.popleft()\nprint(list(queue))";
 if(/hash|dictionary|map/.test(topic))return "scores = {'Ali': 90, 'Maya': 84}\nscores['Omar'] = 95\n\nfor name, score in scores.items():\n    print(name, score)";
 if(/recurs/.test(topic))return "def factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))";
 if(/binary.*search/.test(topic))return "def binary_search(nums, target):\n    left = 0\n    right = len(nums) - 1\n\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n\nprint(binary_search([2, 4, 7, 9, 13], 9))";
 if(/linear.*search|search/.test(topic))return "def linear_search(nums, target):\n    for i in range(len(nums)):\n        if nums[i] == target:\n            return i\n    return -1\n\nprint(linear_search([5, 8, 12, 20], 12))";
 if(/bubble/.test(topic))return "nums = [5, 1, 4, 2, 8]\nfor i in range(len(nums)):\n    for j in range(0, len(nums) - i - 1):\n        if nums[j] > nums[j + 1]:\n            nums[j], nums[j + 1] = nums[j + 1], nums[j]\nprint(nums)";
 if(/selection/.test(topic))return "nums = [64, 25, 12, 22, 11]\nfor i in range(len(nums)):\n    smallest = i\n    for j in range(i + 1, len(nums)):\n        if nums[j] < nums[smallest]:\n            smallest = j\n    nums[i], nums[smallest] = nums[smallest], nums[i]\nprint(nums)";
 if(/insertion/.test(topic))return "nums = [5, 2, 4, 6, 1, 3]\nfor i in range(1, len(nums)):\n    key = nums[i]\n    j = i - 1\n    while j >= 0 and nums[j] > key:\n        nums[j + 1] = nums[j]\n        j -= 1\n    nums[j + 1] = key\nprint(nums)";
 if(/merge/.test(topic))return "def merge_sort(nums):\n    if len(nums) <= 1:\n        return nums\n    mid = len(nums) // 2\n    left = merge_sort(nums[:mid])\n    right = merge_sort(nums[mid:])\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i]); i += 1\n        else:\n            result.append(right[j]); j += 1\n    return result + left[i:] + right[j:]\n\nprint(merge_sort([8, 3, 2, 9, 7, 1]))";
 if(/quick/.test(topic))return "def quick_sort(nums):\n    if len(nums) <= 1:\n        return nums\n    pivot = nums[-1]\n    left = [x for x in nums[:-1] if x <= pivot]\n    right = [x for x in nums[:-1] if x > pivot]\n    return quick_sort(left) + [pivot] + quick_sort(right)\n\nprint(quick_sort([8, 3, 2, 9, 7, 1]))";
 if(/tree|bst/.test(topic))return "class Node:\n    def __init__(self, value):\n        self.value = value\n        self.left = None\n        self.right = None\n\nroot = Node(8)\nroot.left = Node(3)\nroot.right = Node(10)\nprint(root.left.value, root.value, root.right.value)";
 if(/graph|bfs/.test(topic))return "from collections import deque\n\ngraph = [[1, 2], [0, 3], [0, 3], [1, 2]]\nqueue = deque([0])\nseen = {0}\nwhile queue:\n    node = queue.popleft()\n    print(node)\n    for nxt in graph[node]:\n        if nxt not in seen:\n            seen.add(nxt)\n            queue.append(nxt)";
 if(/heap|priority/.test(topic))return "import heapq\n\nheap = []\nfor value in [30, 10, 20]:\n    heapq.heappush(heap, value)\nwhile heap:\n    print(heapq.heappop(heap))";
 if(/two.?pointer/.test(topic))return "nums = [1, 2, 4, 7, 11]\ntarget = 9\nleft = 0\nright = len(nums) - 1\nwhile left < right:\n    total = nums[left] + nums[right]\n    if total == target:\n        print(left, right)\n        break\n    if total < target:\n        left += 1\n    else:\n        right -= 1";
 if(/sliding/.test(topic))return "nums = [2, 1, 5, 1, 3, 2]\nk = 3\nwindow = sum(nums[:k])\nbest = window\nfor i in range(k, len(nums)):\n    window += nums[i] - nums[i - k]\n    best = max(best, window)\nprint(best)";
 return "nums = [10, 20, 30, 40]\nfor i in range(len(nums)):\n    print(nums[i])";
}
function cppProgram(topic){
 if(/linked/.test(topic))return '#include <iostream>\nusing namespace std;\n\nstruct Node {\n    int value;\n    Node* next;\n};\n\nint main() {\n    Node third{30, nullptr};\n    Node second{20, &third};\n    Node first{10, &second};\n\n    Node* current = &first;\n    while (current != nullptr) {\n        cout << current->value << endl;\n        current = current->next;\n    }\n    return 0;\n}';
 if(/stack/.test(topic))return '#include <iostream>\n#include <stack>\nusing namespace std;\n\nint main() {\n    stack<int> values;\n    values.push(10);\n    values.push(20);\n    values.push(30);\n\n    cout << values.top() << endl;\n    values.pop();\n    cout << values.top() << endl;\n    return 0;\n}';
 if(/queue/.test(topic))return '#include <iostream>\n#include <queue>\nusing namespace std;\n\nint main() {\n    queue<int> values;\n    values.push(10);\n    values.push(20);\n    values.push(30);\n\n    cout << values.front() << endl;\n    values.pop();\n    cout << values.front() << endl;\n    return 0;\n}';
 if(/hash|dictionary|map/.test(topic))return '#include <iostream>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n\nint main() {\n    unordered_map<string, int> scores;\n    scores["Ali"] = 90;\n    scores["Maya"] = 84;\n    scores["Omar"] = 95;\n\n    for (auto it = scores.begin(); it != scores.end(); it++) {\n        cout << it->first << " " << it->second << endl;\n    }\n    return 0;\n}';
 if(/recurs/.test(topic))return '#include <iostream>\nusing namespace std;\n\nint factorial(int n) {\n    if (n <= 1) {\n        return 1;\n    }\n    return n * factorial(n - 1);\n}\n\nint main() {\n    cout << factorial(5) << endl;\n    return 0;\n}';
 if(/binary.*search/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint binarySearch(const vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (nums[mid] == target) return mid;\n        if (nums[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n    return -1;\n}\n\nint main() {\n    vector<int> nums = {2, 4, 7, 9, 13};\n    cout << binarySearch(nums, 9) << endl;\n    return 0;\n}';
 if(/linear.*search|search/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint linearSearch(const vector<int>& nums, int target) {\n    for (int i = 0; i < nums.size(); i++) {\n        if (nums[i] == target) return i;\n    }\n    return -1;\n}\n\nint main() {\n    vector<int> nums = {5, 8, 12, 20};\n    cout << linearSearch(nums, 12) << endl;\n    return 0;\n}';
 if(/bubble/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {5, 1, 4, 2, 8};\n    for (int i = 0; i < nums.size(); i++) {\n        for (int j = 0; j < nums.size() - i - 1; j++) {\n            if (nums[j] > nums[j + 1]) swap(nums[j], nums[j + 1]);\n        }\n    }\n    for (int i = 0; i < nums.size(); i++) cout << nums[i] << " ";\n    return 0;\n}';
 if(/selection/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {64, 25, 12, 22, 11};\n    for (int i = 0; i < nums.size(); i++) {\n        int smallest = i;\n        for (int j = i + 1; j < nums.size(); j++) {\n            if (nums[j] < nums[smallest]) smallest = j;\n        }\n        swap(nums[i], nums[smallest]);\n    }\n    for (int i = 0; i < nums.size(); i++) cout << nums[i] << " ";\n    return 0;\n}';
 if(/insertion/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {5, 2, 4, 6, 1, 3};\n    for (int i = 1; i < nums.size(); i++) {\n        int key = nums[i];\n        int j = i - 1;\n        while (j >= 0 && nums[j] > key) {\n            nums[j + 1] = nums[j];\n            j--;\n        }\n        nums[j + 1] = key;\n    }\n    for (int i = 0; i < nums.size(); i++) cout << nums[i] << " ";\n    return 0;\n}';
 if(/merge/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nvoid mergeParts(vector<int>& nums, int left, int mid, int right) {\n    vector<int> temp;\n    int i = left;\n    int j = mid + 1;\n    while (i <= mid && j <= right) {\n        if (nums[i] <= nums[j]) temp.push_back(nums[i++]);\n        else temp.push_back(nums[j++]);\n    }\n    while (i <= mid) temp.push_back(nums[i++]);\n    while (j <= right) temp.push_back(nums[j++]);\n    for (int k = 0; k < temp.size(); k++) nums[left + k] = temp[k];\n}\n\nvoid mergeSort(vector<int>& nums, int left, int right) {\n    if (left >= right) return;\n    int mid = left + (right - left) / 2;\n    mergeSort(nums, left, mid);\n    mergeSort(nums, mid + 1, right);\n    mergeParts(nums, left, mid, right);\n}\n\nint main() {\n    vector<int> nums = {8, 3, 2, 9, 7, 1};\n    mergeSort(nums, 0, nums.size() - 1);\n    for (int i = 0; i < nums.size(); i++) cout << nums[i] << " ";\n    return 0;\n}';
 if(/quick/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint partitionValues(vector<int>& nums, int low, int high) {\n    int pivot = nums[high];\n    int i = low - 1;\n    for (int j = low; j < high; j++) {\n        if (nums[j] <= pivot) { i++; swap(nums[i], nums[j]); }\n    }\n    swap(nums[i + 1], nums[high]);\n    return i + 1;\n}\n\nvoid quickSort(vector<int>& nums, int low, int high) {\n    if (low >= high) return;\n    int pivotIndex = partitionValues(nums, low, high);\n    quickSort(nums, low, pivotIndex - 1);\n    quickSort(nums, pivotIndex + 1, high);\n}\n\nint main() {\n    vector<int> nums = {8, 3, 2, 9, 7, 1};\n    quickSort(nums, 0, nums.size() - 1);\n    for (int i = 0; i < nums.size(); i++) cout << nums[i] << " ";\n    return 0;\n}';
 if(/tree|bst/.test(topic))return '#include <iostream>\nusing namespace std;\n\nstruct Node {\n    int value;\n    Node* left;\n    Node* right;\n};\n\nint main() {\n    Node left{3, nullptr, nullptr};\n    Node right{10, nullptr, nullptr};\n    Node root{8, &left, &right};\n    cout << root.left->value << " " << root.value << " " << root.right->value << endl;\n    return 0;\n}';
 if(/graph|bfs/.test(topic))return '#include <iostream>\n#include <queue>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<vector<int>> graph = {{1, 2}, {0, 3}, {0, 3}, {1, 2}};\n    vector<bool> seen(graph.size(), false);\n    queue<int> q;\n    q.push(0);\n    seen[0] = true;\n    while (!q.empty()) {\n        int node = q.front();\n        q.pop();\n        cout << node << endl;\n        for (int i = 0; i < graph[node].size(); i++) {\n            int next = graph[node][i];\n            if (!seen[next]) { seen[next] = true; q.push(next); }\n        }\n    }\n    return 0;\n}';
 if(/heap|priority/.test(topic))return '#include <iostream>\n#include <queue>\n#include <vector>\nusing namespace std;\n\nint main() {\n    priority_queue<int, vector<int>, greater<int>> heap;\n    heap.push(30);\n    heap.push(10);\n    heap.push(20);\n    while (!heap.empty()) {\n        cout << heap.top() << endl;\n        heap.pop();\n    }\n    return 0;\n}';
 if(/two.?pointer/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {1, 2, 4, 7, 11};\n    int target = 9;\n    int left = 0;\n    int right = nums.size() - 1;\n    while (left < right) {\n        int total = nums[left] + nums[right];\n        if (total == target) { cout << left << " " << right << endl; break; }\n        if (total < target) left++;\n        else right--;\n    }\n    return 0;\n}';
 if(/sliding/.test(topic))return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {2, 1, 5, 1, 3, 2};\n    int k = 3;\n    int window = 0;\n    for (int i = 0; i < k; i++) window += nums[i];\n    int best = window;\n    for (int i = k; i < nums.size(); i++) {\n        window += nums[i] - nums[i - k];\n        if (window > best) best = window;\n    }\n    cout << best << endl;\n    return 0;\n}';
 return '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n\n    for (int i = 0; i < nums.size(); i++) {\n        cout << nums[i] << endl;\n    }\n    return 0;\n}';
}
function loopDeep(topic){if(!/loop|iterat|array|vector|list/.test(topic))return'';return '<div class="csai-loop-deep"><b>Three C++ for-loop possibilities</b><p><b>1. Normal loop (default):</b> <code>for (int i = 0; i &lt; nums.size(); i++)</code>. Initialization runs once, the condition is checked before every iteration, the body runs, then <code>i++</code> updates the index.</p><p><b>2. Range-based loop:</b> <code>for (int value : nums)</code>. Use it when you need each value and do not need the numeric index.</p><p><b>3. Iterator loop:</b> <code>for (auto it = nums.begin(); it != nums.end(); it++)</code>. The iterator points at an element; <code>*it</code> gets that value.</p><p><b>Iterator vs iterative:</b> iterative means repeating steps; an iterator is an object used to move through a container.</p></div>';}
function addStyle(){if(document.getElementById('csai-lesson-language-variants-style'))return;var s=document.createElement('style');s.id='csai-lesson-language-variants-style';s.textContent='[data-csai-source-example],[data-csai-source-host]{display:none!important}.csai-language-example-pair{display:grid;gap:12px;margin:10px 0 16px}.csai-lang-variant{border:1px solid var(--border);border-radius:12px;overflow:hidden;background:var(--panel)}.csai-lang-variant-head{padding:9px 12px;border-bottom:1px solid var(--border);font-weight:900}.csai-language-code{display:block;width:100%;margin:0;padding:13px;white-space:pre;overflow:auto;border:0;background:#0b111b;color:#f4f7fb;font:500 13px/1.55 ui-monospace,SFMono-Regular,Consolas,monospace}.csai-loop-deep{padding:12px 14px;border-top:1px solid var(--border);background:var(--bg);font-size:.86rem;line-height:1.6}.csai-loop-deep p{margin:7px 0}.csai-language-example-pair.dual{grid-template-columns:1fr 1fr}@media(max-width:900px){.csai-language-example-pair.dual{grid-template-columns:1fr}}';document.head.appendChild(s);}
function makePair(pre,lesson,index){var source=String(pre.textContent||''),topic=(topicText(lesson)+' '+source.toLowerCase()),python=looksPython(source)?source:pyProgram(topic),cpp=looksCpp(source)?source:cppProgram(topic),host=pre.closest('.lesson-run-card')||pre;pre.setAttribute('data-csai-source-example','');pre.classList.remove('code');if(host!==pre)host.setAttribute('data-csai-source-host','');else pre.hidden=true;var pair=document.createElement('div');pair.className='csai-language-example-pair';pair.setAttribute('data-csai-language-example-pair','');pair.setAttribute('data-example-index',String(index));pair.innerHTML='<section class="csai-lang-variant" data-lang-variant="python"><div class="csai-lang-variant-head">Python example</div><pre class="csai-language-code" data-csai-language-generated="python">'+esc(python)+'</pre></section><section class="csai-lang-variant" data-lang-variant="cpp"><div class="csai-lang-variant-head">C++ example</div><pre class="csai-language-code" data-csai-language-generated="cpp">'+esc(cpp)+'</pre>'+loopDeep(topic)+'</section>';host.insertAdjacentElement('afterend',pair);return pair;}
function setPair(pair,m){var py=pair.querySelector('[data-lang-variant="python"]'),cpp=pair.querySelector('[data-lang-variant="cpp"]'),pyCode=py&&py.querySelector('pre');pair.classList.toggle('dual',m==='dual');if(py)py.style.display=m==='cpp'?'none':'';if(cpp)cpp.style.display=m==='python'?'none':'';if(pyCode)pyCode.classList.toggle('csai-hidden-python-example',m==='cpp');}
function apply(){if(!flexible())return;addStyle();document.querySelectorAll('[data-cpp-lesson-panel],[data-cpp-generic-note]').forEach(function(n){n.remove();});document.querySelectorAll('.lesson').forEach(function(lesson){var sources=Array.from(lesson.querySelectorAll('.body pre.code')).filter(function(pre){return !pre.hasAttribute('data-csai-language-generated')&&!pre.hasAttribute('data-csai-source-example');});sources.forEach(function(pre,i){makePair(pre,lesson,i);});});var m=mode();document.querySelectorAll('[data-csai-language-example-pair]').forEach(function(pair){setPair(pair,m);});}
function queue(){clearTimeout(timer);timer=setTimeout(apply,30);}
window.CSAILessonLanguageVariants={apply:apply};
if(flexible()){apply();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply,{once:true});window.addEventListener('csai-language-mode-change',function(){setTimeout(apply,0);setTimeout(apply,120);});window.addEventListener('csai-language-controller-applied',function(){setTimeout(apply,0);});var observer=new MutationObserver(function(records){if(records.some(function(r){return r.addedNodes&&r.addedNodes.length;}))queue();});observer.observe(document.documentElement,{childList:true,subtree:true});}
})();
