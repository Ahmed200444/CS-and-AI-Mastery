# Expense tracker

> Store expenses in a dict/list, categorize them, and print monthly totals from user input.

## Overview
Store expenses in a dict/list, categorize them, and print monthly totals from user input. This project applies concepts from **Python** in a working implementation.

## Tech stack
- **Primary language:** Python
- **Detected dependencies/modules:** `json`, `datetime`
- **Learning context:** Python · CS & AI Mastery

## What this project demonstrates
- Complete the project implementation
- Validate the important behavior
- Handle realistic edge cases

## Implementation snapshot
- **Main file:** `expense-tracker.py`
- **Non-empty code lines:** 116
- **Detected functions:** 10
- **Detected classes/components:** 0

## Architecture
The main implementation lives in `expense-tracker.py`. Detected functions include `load_expenses`, `save_expenses`, `get_amount`, `add_expense`, `view_expenses`, `show_total`. The README describes only evidence visible in the current project workspace.

## How to run
```bash
python expense-tracker.py
```

## Validation
Validate the project with automated tests, representative inputs, edge cases, and a clean local run before publishing.

## Runtime result
Run the project locally and verify the expected behavior with representative inputs and edge cases.

## Engineering decisions
- The implementation is decomposed into focused functions such as `load_expenses`, `save_expenses`, `get_amount`.
- Explicit error handling is present so failing paths are handled instead of silently ignored.

## Next improvements
- Add a dedicated automated test suite for normal, boundary, and failure cases.

## Course context
Built as part of **Python** in **CS & AI Mastery**. This README is generated from the current project code and requirements.
