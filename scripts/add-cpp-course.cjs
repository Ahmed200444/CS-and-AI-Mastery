const fs=require('fs');
const path=require('path');

const indexPath=path.join(process.cwd(),'index.html');
if(!fs.existsSync(indexPath))throw new Error('index.html is missing');
let html=fs.readFileSync(indexPath,'utf8');

function lesson(id,title,explanation,concepts,examples,commonMistakes=[]){
  return {id,title,explanation,concepts,examples,commonMistakes};
}
function ex(id,title,prompt,hint,starter,solution,difficulty=1){
  return {id,title,prompt,hint,starter,solution,difficulty,type:'coding-practice'};
}

const lessons=[
lesson('cpp-01','How C++ Programs Work','C++ is compiled: source code is translated into machine/WASM instructions before it runs. A complete beginner program normally includes headers, a main function, statements ending with semicolons, and return 0 to report successful completion.',['source file','compiler','main()','statements','semicolon'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << "Hello, C++!" << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << "First line" << endl;\n    cout << "Second line" << endl;\n    return 0;\n}`
],['Forgetting the semicolon at the end of a statement.','Writing code outside main() before learning when that is allowed.']),
lesson('cpp-02','Variables and Data Types','Variables give names to values. C++ is statically typed, so the type controls what kind of value a variable stores and what operations are valid.',['int','double','char','bool','string','type safety'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    int age = 20;\n    double gpa = 3.75;\n    char grade = 'A';\n    bool enrolled = true;\n    string name = "Ahmed";\n    cout << name << " " << age << " " << gpa << " " << grade << " " << enrolled << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int whole = 7;\n    double precise = 7.5;\n    cout << whole + 3 << endl;\n    cout << precise / 2.0 << endl;\n    return 0;\n}`
],['Using double quotes for a char instead of single quotes.','Expecting integer division such as 7/2 to produce 3.5.']),
lesson('cpp-03','Input and Output with cin and cout','cout sends data to standard output. cin reads typed input into variables. The stream operators << and >> show the direction data moves.',['cout','cin','<<','>>','endl'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string name = "Student";\n    int year = 2;\n    cout << "Name: " << name << endl;\n    cout << "Year: " << year << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <sstream>\nusing namespace std;\n\nint main() {\n    istringstream input("12 8");\n    int a, b;\n    input >> a >> b;\n    cout << "Sum = " << a + b << endl;\n    return 0;\n}`
],['Reading a full name with cin >> name when the input contains spaces.','Mixing cin >> with getline without handling the leftover newline.']),
lesson('cpp-04','Operators and Expressions','Expressions combine values and operators to produce a result. Learn arithmetic, comparison, logical, assignment, and increment operators, plus precedence and parentheses.',['arithmetic','comparison','logical operators','assignment','precedence'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int a = 10, b = 3;\n    cout << a + b << endl;\n    cout << a - b << endl;\n    cout << a * b << endl;\n    cout << a / b << endl;\n    cout << a % b << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 82;\n    bool passed = score >= 60;\n    bool distinction = score >= 80 && score <= 100;\n    cout << passed << " " << distinction << endl;\n    return 0;\n}`
],['Using = when you mean ==.','Forgetting that integer division removes the fractional part.']),
lesson('cpp-05','if, else if, and else','Conditional statements choose which block runs. Conditions are evaluated from top to bottom; once a matching branch runs, later branches in the same chain are skipped.',['if','else if','else','conditions','branching'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 76;\n    if (score >= 90) {\n        cout << "A" << endl;\n    } else if (score >= 80) {\n        cout << "B" << endl;\n    } else if (score >= 70) {\n        cout << "C" << endl;\n    } else {\n        cout << "Needs improvement" << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int number = -4;\n    if (number > 0) {\n        cout << "positive" << endl;\n    } else if (number < 0) {\n        cout << "negative" << endl;\n    } else {\n        cout << "zero" << endl;\n    }\n    return 0;\n}`
],['Putting the broadest condition first so a more specific condition can never be reached.']),
lesson('cpp-06','switch Statements','switch is useful when one expression is compared with several discrete constant cases. break normally prevents execution from falling through into the next case.',['switch','case','break','default','fall-through'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 3;\n    switch (day) {\n        case 1: cout << "Monday" << endl; break;\n        case 2: cout << "Tuesday" << endl; break;\n        case 3: cout << "Wednesday" << endl; break;\n        default: cout << "Other day" << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    char operation = '*';\n    int a = 4, b = 5;\n    switch (operation) {\n        case '+': cout << a + b << endl; break;\n        case '-': cout << a - b << endl; break;\n        case '*': cout << a * b << endl; break;\n        default: cout << "Unsupported" << endl;\n    }\n    return 0;\n}`
],['Forgetting break when fall-through was not intended.']),
lesson('cpp-07','for Loops: Index, Range, and Iterator','C++ has several useful forms of for loop. The normal index loop is the best starting point because it exposes initialization, condition, update, and indexing. Range-based loops are concise when only values matter. Iterator loops teach how STL containers are traversed.',['normal for loop','range-based for','iterator loop','indexing','iteration trace'],[
`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    for (int i = 0; i < nums.size(); i++) {\n        cout << "index " << i << " -> " << nums[i] << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    cout << "Range-based:" << endl;\n    for (int value : nums) {\n        cout << value << endl;\n    }\n    cout << "Iterator-based:" << endl;\n    for (auto it = nums.begin(); it != nums.end(); it++) {\n        cout << *it << endl;\n    }\n    return 0;\n}`
],['Using <= nums.size() and accessing one position past the end.','Using a range-based loop when you actually need the index.']),
lesson('cpp-08','while and do-while Loops','while repeats while a condition is true and checks before the body. do-while checks after the body, so it executes at least once.',['while','do-while','loop condition','counter','termination'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int count = 1;\n    while (count <= 5) {\n        cout << count << endl;\n        count++;\n    }\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int number = 3;\n    do {\n        cout << "number = " << number << endl;\n        number--;\n    } while (number > 0);\n    return 0;\n}`
],['Forgetting to change the variable used by the condition, creating an infinite loop.']),
lesson('cpp-09','Functions','Functions package reusable behavior. A function has a return type, name, parameters, body, and optional return statement.',['function declaration','parameters','arguments','return type','return value'],[
`#include <iostream>\nusing namespace std;\n\nint add(int a, int b) {\n    return a + b;\n}\n\nint main() {\n    cout << add(4, 7) << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nvoid greet(const string& name) {\n    cout << "Hello, " << name << "!" << endl;\n}\n\nint main() {\n    greet("C++ learner");\n    return 0;\n}`
],['Declaring one return type but returning an incompatible value.','Confusing parameters with the arguments passed during a call.']),
lesson('cpp-10','Scope, References, and const','Scope controls where names exist. References let a parameter refer to the caller’s original object. const communicates that a value should not be changed through that name.',['local scope','reference','const reference','pass by value','pass by reference'],[
`#include <iostream>\nusing namespace std;\n\nvoid addOne(int& value) {\n    value++;\n}\n\nint main() {\n    int number = 9;\n    addOne(number);\n    cout << number << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid show(const string& text) {\n    cout << text << endl;\n}\n\nint main() {\n    string message = "No copy needed";\n    show(message);\n    return 0;\n}`
],['Returning a reference to a local variable that stops existing after the function returns.']),
lesson('cpp-11','Arrays','A fixed-size array stores elements of one type in contiguous positions. Indexing begins at zero.',['array','fixed size','index','bounds','contiguous storage'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[5] = {72, 88, 91, 65, 84};\n    for (int i = 0; i < 5; i++) {\n        cout << scores[i] << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int values[4] = {3, 7, 2, 9};\n    int total = 0;\n    for (int i = 0; i < 4; i++) {\n        total += values[i];\n    }\n    cout << "total = " << total << endl;\n    return 0;\n}`
],['Accessing an index below 0 or at/above the array size.']),
lesson('cpp-12','std::string','std::string manages text safely and provides length, indexing, concatenation, searching, and substring operations.',['string','size()','indexing','find','substr'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string word = "compiler";\n    cout << word.size() << endl;\n    cout << word[0] << endl;\n    cout << word.substr(0, 4) << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string first = "C++";\n    string second = " course";\n    string combined = first + second;\n    cout << combined << endl;\n    cout << combined.find("course") << endl;\n    return 0;\n}`
],['Using an invalid string index.','Forgetting that find returns string::npos when the text is not found.']),
lesson('cpp-13','std::vector','vector is a resizable array from the Standard Library. It tracks its size and supports push_back, pop_back, indexing, and iteration.',['vector','push_back','pop_back','size','dynamic sequence'],[
`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {5, 10, 15};\n    nums.push_back(20);\n    for (int i = 0; i < nums.size(); i++) {\n        cout << nums[i] << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {2, 4, 6, 8};\n    int total = 0;\n    for (int value : nums) {\n        total += value;\n    }\n    cout << "average = " << static_cast<double>(total) / nums.size() << endl;\n    return 0;\n}`
],['Assuming vector indexing performs automatic bounds checking with operator[].']),
lesson('cpp-14','Pointers','A pointer stores a memory address. & obtains an address and * dereferences a pointer to access the object at that address.',['pointer','address','dereference','nullptr','memory'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int value = 42;\n    int* ptr = &value;\n    cout << value << endl;\n    cout << *ptr << endl;\n    *ptr = 50;\n    cout << value << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int* ptr = nullptr;\n    if (ptr == nullptr) {\n        cout << "pointer is safely empty" << endl;\n    }\n    return 0;\n}`
],['Dereferencing nullptr or a pointer that no longer points to a valid object.']),
lesson('cpp-15','Dynamic Memory and RAII','new/delete expose manual dynamic allocation, but modern C++ prefers ownership types such as unique_ptr because they clean up automatically through RAII.',['new','delete','heap','RAII','unique_ptr'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int* number = new int(25);\n    cout << *number << endl;\n    delete number;\n    number = nullptr;\n    return 0;\n}`,
`#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> number = make_unique<int>(25);\n    cout << *number << endl;\n    return 0;\n}`
],['Forgetting delete after new.','Using a pointer after the pointed-to object has been deleted.']),
lesson('cpp-16','structs','A struct creates a custom type that groups related fields. In C++, structs can also have member functions; their members are public by default.',['struct','fields','custom type','member access','aggregate'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Student {\n    string name;\n    int id;\n    double gpa;\n};\n\nint main() {\n    Student s{"Maya", 101, 3.8};\n    cout << s.name << " " << s.gpa << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nstruct Point {\n    int x;\n    int y;\n    int sum() const { return x + y; }\n};\n\nint main() {\n    Point p{3, 4};\n    cout << p.sum() << endl;\n    return 0;\n}`
],['Confusing the type name with an object created from that type.']),
lesson('cpp-17','Classes and Objects','A class combines state and behavior. Objects are instances of a class. private data protects invariants while public methods define the supported interface.',['class','object','private','public','methods'],[
`#include <iostream>\nusing namespace std;\n\nclass Counter {\nprivate:\n    int value = 0;\npublic:\n    void increment() { value++; }\n    int getValue() const { return value; }\n};\n\nint main() {\n    Counter c;\n    c.increment();\n    c.increment();\n    cout << c.getValue() << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nclass Rectangle {\npublic:\n    double width = 0;\n    double height = 0;\n    double area() const { return width * height; }\n};\n\nint main() {\n    Rectangle r;\n    r.width = 5;\n    r.height = 3;\n    cout << r.area() << endl;\n    return 0;\n}`
],['Trying to access a private member directly from outside the class.']),
lesson('cpp-18','Constructors and Destructors','Constructors establish a valid initial state when an object is created. Destructors run when an object’s lifetime ends and are important for resource management.',['constructor','initializer list','destructor','object lifetime','RAII'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nclass User {\n    string name;\npublic:\n    User(const string& n) : name(n) {}\n    string getName() const { return name; }\n};\n\nint main() {\n    User u("Noor");\n    cout << u.getName() << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nclass Trace {\npublic:\n    Trace() { cout << "constructed" << endl; }\n    ~Trace() { cout << "destroyed" << endl; }\n};\n\nint main() {\n    Trace t;\n    cout << "inside main" << endl;\n    return 0;\n}`
],['Assigning members in the constructor body when an initializer list is clearer or required.']),
lesson('cpp-19','Encapsulation and Validation','Encapsulation keeps representation details private and exposes controlled operations. Setters can reject invalid state instead of allowing arbitrary writes.',['encapsulation','getter','setter','invariant','validation'],[
`#include <iostream>\nusing namespace std;\n\nclass BankAccount {\n    double balance = 0;\npublic:\n    void deposit(double amount) {\n        if (amount > 0) balance += amount;\n    }\n    double getBalance() const { return balance; }\n};\n\nint main() {\n    BankAccount account;\n    account.deposit(120.5);\n    account.deposit(-20);\n    cout << account.getBalance() << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nclass Temperature {\n    double celsius = 0;\npublic:\n    bool setCelsius(double value) {\n        if (value < -273.15) return false;\n        celsius = value;\n        return true;\n    }\n    double getCelsius() const { return celsius; }\n};\n\nint main() {\n    Temperature t;\n    cout << t.setCelsius(-300) << endl;\n    cout << t.setCelsius(24) << " " << t.getCelsius() << endl;\n    return 0;\n}`
],['Creating getters and setters that allow invalid state with no validation.']),
lesson('cpp-20','Inheritance','Inheritance lets a derived class reuse and extend accessible behavior from a base class. Use it when the relationship really represents an is-a relationship.',['base class','derived class','public inheritance','protected','is-a'],[
`#include <iostream>\n#include <string>\nusing namespace std;\n\nclass Person {\npublic:\n    string name;\n    Person(const string& n) : name(n) {}\n};\n\nclass Student : public Person {\npublic:\n    int id;\n    Student(const string& n, int i) : Person(n), id(i) {}\n};\n\nint main() {\n    Student s("Lina", 44);\n    cout << s.name << " " << s.id << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nclass Vehicle {\npublic:\n    void move() const { cout << "moving" << endl; }\n};\n\nclass Car : public Vehicle {\npublic:\n    void honk() const { cout << "beep" << endl; }\n};\n\nint main() {\n    Car c;\n    c.move();\n    c.honk();\n    return 0;\n}`
],['Using inheritance only to reuse code when composition would model the relationship more accurately.']),
lesson('cpp-21','Polymorphism and virtual Functions','Runtime polymorphism lets code work through a base pointer/reference while a virtual function dispatches to the derived implementation. A polymorphic base should normally have a virtual destructor.',['virtual','override','base reference','dynamic dispatch','virtual destructor'],[
`#include <iostream>\nusing namespace std;\n\nclass Animal {\npublic:\n    virtual ~Animal() = default;\n    virtual void speak() const { cout << "animal" << endl; }\n};\nclass Dog : public Animal {\npublic:\n    void speak() const override { cout << "woof" << endl; }\n};\n\nint main() {\n    Dog dog;\n    Animal& a = dog;\n    a.speak();\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\n#include <memory>\nusing namespace std;\n\nclass Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0;\n};\nclass Square : public Shape {\n    double side;\npublic:\n    Square(double s) : side(s) {}\n    double area() const override { return side * side; }\n};\n\nint main() {\n    vector<unique_ptr<Shape>> shapes;\n    shapes.push_back(make_unique<Square>(4));\n    for (const auto& shape : shapes) {\n        cout << shape->area() << endl;\n    }\n    return 0;\n}`
],['Forgetting virtual when you expect runtime dispatch through a base reference or pointer.']),
lesson('cpp-22','Templates','Templates let one algorithm or type work with multiple compatible types while preserving compile-time type checking.',['function template','type parameter','generic programming','compile time'],[
`#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT bigger(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << bigger(4, 9) << endl;\n    cout << bigger(2.5, 1.7) << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nclass Box {\n    T value;\npublic:\n    Box(T v) : value(v) {}\n    T get() const { return value; }\n};\n\nint main() {\n    Box<int> a(12);\n    Box<double> b(3.5);\n    cout << a.get() << " " << b.get() << endl;\n    return 0;\n}`
],['Assuming every type supports every operation used inside a template.']),
lesson('cpp-23','STL Containers: stack, queue, map, and set','The Standard Template Library provides containers with different behaviors. Choose a container based on the operations you need rather than using vector for everything.',['stack','queue','map','unordered_map','set'],[
`#include <iostream>\n#include <stack>\n#include <queue>\nusing namespace std;\n\nint main() {\n    stack<int> s;\n    s.push(10); s.push(20);\n    cout << s.top() << endl;\n    queue<int> q;\n    q.push(10); q.push(20);\n    cout << q.front() << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <unordered_map>\n#include <set>\n#include <string>\nusing namespace std;\n\nint main() {\n    unordered_map<string, int> counts;\n    counts["cpp"]++;\n    counts["cpp"]++;\n    set<int> uniqueValues = {3, 1, 3, 2};\n    cout << counts["cpp"] << " " << uniqueValues.size() << endl;\n    return 0;\n}`
],['Using operator[] on a map without realizing it inserts a missing key.']),
lesson('cpp-24','Iterators and Algorithms','Iterators connect containers with generic STL algorithms. Algorithms such as sort, find, count, and reverse work on iterator ranges.',['iterator','begin','end','sort','find','algorithm'],[
`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {7, 2, 9, 1};\n    sort(nums.begin(), nums.end());\n    for (int value : nums) {\n        cout << value << " ";\n    }\n    cout << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {4, 8, 15, 16, 23, 42};\n    auto it = find(nums.begin(), nums.end(), 15);\n    if (it != nums.end()) {\n        cout << "found at index " << distance(nums.begin(), it) << endl;\n    }\n    return 0;\n}`
],['Dereferencing end(), which does not refer to a real element.']),
lesson('cpp-25','Exceptions','Exceptions separate error signaling from normal return values. throw creates an exception; try marks code that may throw; catch handles matching exception types.',['try','throw','catch','runtime_error','exception safety'],[
`#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\ndouble divide(double a, double b) {\n    if (b == 0) throw runtime_error("division by zero");\n    return a / b;\n}\n\nint main() {\n    try {\n        cout << divide(10, 2) << endl;\n    } catch (const exception& e) {\n        cout << e.what() << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nint ageNextYear(int age) {\n    if (age < 0) throw invalid_argument("age cannot be negative");\n    return age + 1;\n}\n\nint main() {\n    try {\n        cout << ageNextYear(-2) << endl;\n    } catch (const invalid_argument& e) {\n        cout << "caught: " << e.what() << endl;\n    }\n    return 0;\n}`
],['Catching every error with catch(...) before learning which exception you actually expect.']),
lesson('cpp-26','File Input and Output','fstream classes let programs read and write files. The same stream ideas used by cin/cout also apply to ifstream and ofstream.',['ifstream','ofstream','fstream','open','close'],[
`#include <iostream>\n#include <fstream>\n#include <string>\nusing namespace std;\n\nint main() {\n    const string path = "/tmp/cpp-course-demo.txt";\n    ofstream out(path);\n    out << "C++ file example" << endl;\n    out.close();\n    ifstream in(path);\n    string line;\n    getline(in, line);\n    cout << line << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <sstream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string fileLikeText = "10 20 30";\n    istringstream input(fileLikeText);\n    int value = 0, total = 0;\n    while (input >> value) {\n        total += value;\n    }\n    cout << total << endl;\n    return 0;\n}`
],['Assuming a file opened successfully without checking the stream state in real applications.']),
lesson('cpp-27','Recursion','A recursive function calls itself on a smaller version of the problem. It needs a base case that stops recursion and a recursive step that makes progress toward that base case.',['recursion','base case','recursive case','call stack'],[
`#include <iostream>\nusing namespace std;\n\nlong long factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint main() {\n    cout << factorial(5) << endl;\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint sumTo(int n) {\n    if (n <= 0) return 0;\n    return n + sumTo(n - 1);\n}\n\nint main() {\n    cout << sumTo(5) << endl;\n    return 0;\n}`
],['Writing recursion with no reachable base case.']),
lesson('cpp-28','Lambdas and Modern C++','A lambda is an unnamed function object that can be written close to where it is used. Capture lists control which surrounding values are available.',['lambda','capture','auto','algorithm','modern C++'],[
`#include <iostream>\nusing namespace std;\n\nint main() {\n    auto square = [](int value) { return value * value; };\n    cout << square(6) << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {1, 2, 3, 4, 5, 6};\n    int limit = 3;\n    int count = count_if(nums.begin(), nums.end(), [limit](int value) {\n        return value > limit;\n    });\n    cout << count << endl;\n    return 0;\n}`
],['Capturing by reference when the referenced object will not live long enough.']),
lesson('cpp-29','Debugging and Common C++ Errors','Compiler errors, runtime errors, and logic errors require different debugging strategies. Read the first useful compiler diagnostic, reduce the problem, inspect values, and test boundaries.',['compiler error','runtime error','logic error','debugging','boundary cases'],[
`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {4, 8, 12};\n    for (int i = 0; i < nums.size(); i++) {\n        cout << "i=" << i << " value=" << nums[i] << endl;\n    }\n    return 0;\n}`,
`#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 0;\n    for (int i = 1; i <= 5; i++) {\n        total += i;\n        cout << "after i=" << i << ", total=" << total << endl;\n    }\n    return 0;\n}`
],['Fixing many lines at once instead of isolating the smallest failing case.']),
lesson('cpp-30','Putting C++ Together','A larger C++ program combines functions, classes, containers, validation, iteration, and algorithms. Plan responsibilities first, then build and test one small piece at a time.',['program design','classes','vectors','functions','integration'],[
`#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\nstruct Student {\n    string name;\n    int score;\n};\n\ndouble average(const vector<Student>& students) {\n    int total = 0;\n    for (const Student& student : students) {\n        total += student.score;\n    }\n    return students.empty() ? 0.0 : static_cast<double>(total) / students.size();\n}\n\nint main() {\n    vector<Student> students = {{"A", 80}, {"B", 90}, {"C", 70}};\n    cout << average(students) << endl;\n    return 0;\n}`,
`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> scores = {72, 91, 84, 65, 88};\n    sort(scores.begin(), scores.end());\n    cout << "lowest=" << scores.front() << endl;\n    cout << "highest=" << scores.back() << endl;\n    return 0;\n}`
],['Trying to design the entire program in one giant main function.'])
];

const exercises=[
ex('cpp-ex-01','Sum 1 to N','Write a C++ program that calculates the sum from 1 through n using a normal index/counter for loop. Use n = 10 in your runnable version.','Initialize total to 0 and add i during every iteration.',`#include <iostream>\nusing namespace std;\n\nint main() {\n    int n = 10;\n    int total = 0;\n    // TODO: use a for loop to add 1 through n\n    cout << total << endl;\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\nint main() {\n    int n = 10;\n    int total = 0;\n    for (int i = 1; i <= n; i++) {\n        total += i;\n    }\n    cout << total << endl;\n    return 0;\n}`),
ex('cpp-ex-02','Count Even Values','Count how many values in a vector are even.','Use value % 2 == 0.',`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {2, 7, 8, 11, 14, 19};\n    int count = 0;\n    // TODO\n    cout << count << endl;\n    return 0;\n}`,`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {2, 7, 8, 11, 14, 19};\n    int count = 0;\n    for (int value : nums) {\n        if (value % 2 == 0) count++;\n    }\n    cout << count << endl;\n    return 0;\n}`),
ex('cpp-ex-03','Reverse a String','Print a string in reverse order without using std::reverse.','Start the index at size()-1 and move down.',`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string text = "computer";\n    // TODO\n    return 0;\n}`,`#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string text = "computer";\n    for (int i = static_cast<int>(text.size()) - 1; i >= 0; i--) {\n        cout << text[i];\n    }\n    cout << endl;\n    return 0;\n}`),
ex('cpp-ex-04','Find the Maximum','Find the largest value in a vector without calling max_element.','Start largest with the first element.',`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {9, 3, 17, 6, 12};\n    int largest = nums[0];\n    // TODO\n    cout << largest << endl;\n    return 0;\n}`,`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {9, 3, 17, 6, 12};\n    int largest = nums[0];\n    for (int i = 1; i < nums.size(); i++) {\n        if (nums[i] > largest) largest = nums[i];\n    }\n    cout << largest << endl;\n    return 0;\n}`),
ex('cpp-ex-05','Rectangle Function','Write a function area(width, height) that returns a double.','The function should return width * height.',`#include <iostream>\nusing namespace std;\n\ndouble area(double width, double height) {\n    return 0; // TODO\n}\n\nint main() {\n    cout << area(4.5, 2.0) << endl;\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\ndouble area(double width, double height) {\n    return width * height;\n}\n\nint main() {\n    cout << area(4.5, 2.0) << endl;\n    return 0;\n}`),
ex('cpp-ex-06','Vector Average','Calculate the average of a vector as a double.','Cast the total or size so division is floating point.',`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    int total = 0;\n    // TODO\n    double average = 0;\n    cout << average << endl;\n    return 0;\n}`,`#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    int total = 0;\n    for (int value : nums) total += value;\n    double average = static_cast<double>(total) / nums.size();\n    cout << average << endl;\n    return 0;\n}`),
ex('cpp-ex-07','Rectangle Class','Create a Rectangle class with private width/height, a constructor, and an area() method.','Use an initializer list.',`#include <iostream>\nusing namespace std;\n\nclass Rectangle {\n    // TODO\n};\n\nint main() {\n    // TODO: create a 5 by 3 rectangle and print its area\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\nclass Rectangle {\n    double width;\n    double height;\npublic:\n    Rectangle(double w, double h) : width(w), height(h) {}\n    double area() const { return width * height; }\n};\n\nint main() {\n    Rectangle r(5, 3);\n    cout << r.area() << endl;\n    return 0;\n}`,2),
ex('cpp-ex-08','Safe Bank Account','Create a BankAccount class whose deposit method ignores non-positive deposits.','Keep balance private.',`#include <iostream>\nusing namespace std;\n\nclass BankAccount {\n    // TODO\n};\n\nint main() {\n    // TODO\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\nclass BankAccount {\n    double balance = 0;\npublic:\n    void deposit(double amount) { if (amount > 0) balance += amount; }\n    double getBalance() const { return balance; }\n};\n\nint main() {\n    BankAccount account;\n    account.deposit(100);\n    account.deposit(-50);\n    cout << account.getBalance() << endl;\n    return 0;\n}`,2),
ex('cpp-ex-09','Student Inheritance','Create Student as a public derived class of Person and print inherited name plus student ID.','Call the Person constructor from the Student initializer list.',`#include <iostream>\n#include <string>\nusing namespace std;\n\nclass Person {\npublic:\n    string name;\n    Person(const string& n) : name(n) {}\n};\n\n// TODO: Student\n\nint main() {\n    return 0;\n}`,`#include <iostream>\n#include <string>\nusing namespace std;\n\nclass Person {\npublic:\n    string name;\n    Person(const string& n) : name(n) {}\n};\nclass Student : public Person {\npublic:\n    int id;\n    Student(const string& n, int i) : Person(n), id(i) {}\n};\n\nint main() {\n    Student s("Omar", 77);\n    cout << s.name << " " << s.id << endl;\n    return 0;\n}`,2),
ex('cpp-ex-10','Virtual Shape Area','Use a virtual area() method and call it through a base reference.','Make the base destructor virtual.',`#include <iostream>\nusing namespace std;\n\nclass Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0;\n};\n\n// TODO: Square\n\nint main() {\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\nclass Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0;\n};\nclass Square : public Shape {\n    double side;\npublic:\n    Square(double s) : side(s) {}\n    double area() const override { return side * side; }\n};\n\nint main() {\n    Square square(4);\n    Shape& shape = square;\n    cout << shape.area() << endl;\n    return 0;\n}`,3),
ex('cpp-ex-11','Generic Minimum','Write a function template smaller that returns the smaller of two same-type values.','Use template <typename T>.',`#include <iostream>\nusing namespace std;\n\n// TODO: template function\n\nint main() {\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT smaller(T a, T b) { return a < b ? a : b; }\n\nint main() {\n    cout << smaller(8, 3) << endl;\n    cout << smaller(2.5, 4.1) << endl;\n    return 0;\n}`,2),
ex('cpp-ex-12','Word Frequency Map','Count words in a vector using unordered_map and print each frequency.','Use counts[word]++.',`#include <iostream>\n#include <vector>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n\nint main() {\n    vector<string> words = {"cpp", "python", "cpp", "cpp", "python"};\n    unordered_map<string, int> counts;\n    // TODO\n    return 0;\n}`,`#include <iostream>\n#include <vector>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n\nint main() {\n    vector<string> words = {"cpp", "python", "cpp", "cpp", "python"};\n    unordered_map<string, int> counts;\n    for (const string& word : words) counts[word]++;\n    cout << "cpp=" << counts["cpp"] << endl;\n    cout << "python=" << counts["python"] << endl;\n    return 0;\n}`,2),
ex('cpp-ex-13','Safe Division Exception','Throw runtime_error when the divisor is zero and catch it in main.','Check b == 0 before dividing.',`#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\n// TODO: divide function\n\nint main() {\n    return 0;\n}`,`#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\ndouble divide(double a, double b) {\n    if (b == 0) throw runtime_error("division by zero");\n    return a / b;\n}\n\nint main() {\n    try {\n        cout << divide(8, 0) << endl;\n    } catch (const exception& e) {\n        cout << e.what() << endl;\n    }\n    return 0;\n}`,2),
ex('cpp-ex-14','Recursive Factorial','Implement factorial recursively with a correct base case.','n <= 1 can return 1.',`#include <iostream>\nusing namespace std;\n\nlong long factorial(int n) {\n    return 0; // TODO\n}\n\nint main() {\n    cout << factorial(6) << endl;\n    return 0;\n}`,`#include <iostream>\nusing namespace std;\n\nlong long factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint main() {\n    cout << factorial(6) << endl;\n    return 0;\n}`,2),
ex('cpp-ex-15','Sort and Search','Sort a vector and use find to locate 23. Print the sorted values and whether 23 was found.','Use sort(begin,end) then find(begin,end,23).',`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {42, 8, 23, 4, 15};\n    // TODO\n    return 0;\n}`,`#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {42, 8, 23, 4, 15};\n    sort(nums.begin(), nums.end());\n    for (int value : nums) cout << value << " ";\n    cout << endl;\n    auto it = find(nums.begin(), nums.end(), 23);\n    cout << (it != nums.end() ? "found" : "not found") << endl;\n    return 0;\n}`,2)
];

const quiz=[
{id:'cpp-q-01',question:'Where does execution of a normal C++ program begin?',options:['Inside #include','Inside main()','Inside using namespace std','At the first variable'],answer:1},
{id:'cpp-q-02',question:'What does i++ do in a normal for loop?',options:['Resets i','Increases i by one','Checks the condition','Creates an array'],answer:1},
{id:'cpp-q-03',question:'Which loop is usually simplest when you only need each value from a vector?',options:['range-based for','goto','do-while only','switch'],answer:0},
{id:'cpp-q-04',question:'What does *ptr mean for an int* pointer?',options:['Create a pointer','Dereference the pointer','Delete memory','Compare addresses'],answer:1},
{id:'cpp-q-05',question:'Which container works as last-in, first-out?',options:['queue','stack','set','map'],answer:1},
{id:'cpp-q-06',question:'What is the purpose of a constructor?',options:['Destroy an object','Initialize an object','Sort a vector','Catch an exception'],answer:1},
{id:'cpp-q-07',question:'What keyword enables runtime method dispatch in a base class?',options:['static','virtual','friend','constexpr'],answer:1},
{id:'cpp-q-08',question:'Which header contains std::vector?',options:['<vector>','<arraylist>','<container>','<memory>'],answer:0},
{id:'cpp-q-09',question:'Why use const string& for a read-only large string parameter?',options:['It copies twice','It avoids a copy and prevents modification through that reference','It deletes the string','It converts to char'],answer:1},
{id:'cpp-q-10',question:'What is required for correct recursion?',options:['Only a loop','A reachable base case','A global variable','A pointer'],answer:1},
{id:'cpp-q-11',question:'What does nums.end() represent?',options:['The last element itself','A position just after the last element','The first element','The vector size as an int'],answer:1},
{id:'cpp-q-12',question:'Which modern C++ ownership type automatically deletes a uniquely owned object?',options:['raw int*','unique_ptr','char*','void*'],answer:1}
];

const projects=[
{id:'cpp-proj-01',title:'Console Gradebook',description:'Build a gradebook with a Student struct/class, vector storage, functions for average/min/max, sorting, and clear formatted output.'},
{id:'cpp-proj-02',title:'Inventory Manager',description:'Build a small inventory system with an Item class, encapsulated quantity/price, vector storage, searching, updating, and total inventory value.'},
{id:'cpp-proj-03',title:'Contact Manager',description:'Build a console contact manager using classes, strings, vectors, searching, validation, and optional file persistence.'}
];

const cppCourse={
  id:'cpp',title:'C++',icon:'⚙️',
  blurb:'University-style C++ from fundamentals to OOP, pointers, STL, algorithms, debugging, and complete runnable programs.',
  description:'Learn C++ deeply with multiple runnable examples per lesson, line-by-line reasoning, loop traces and alternatives, assessment practice, Evergreen review, projects, and GitHub publishing.',
  category:'core',level:'Beginner to Intermediate',linked:'C++',optional:false,tier:'core',available:true,status:'available',displayOrder:1.5,prerequisites:[],
  lessons,exercises,quiz,projects,
  capstone:{id:'cpp-capstone',title:'Student Management System Capstone',description:'Combine classes, encapsulation, vectors, functions, searching/sorting, validation, exceptions, and optional file persistence into a complete console student-management program.'}
};

const re=/(<script\b[^>]*\bid=["']coursedata["'][^>]*>)([\s\S]*?)(<\/script>)/i;
const match=html.match(re);
if(!match)throw new Error('coursedata script is missing from index.html');
let courses=JSON.parse(match[2]);
if(!Array.isArray(courses))throw new Error('coursedata is not an array');
courses=courses.filter(c=>c&&c.id!=='cpp');
courses.push(cppCourse);
const safe=JSON.stringify(courses).replace(/<\//g,'<\\/');
html=html.replace(re,`${match[1]}${safe}${match[3]}`);
fs.writeFileSync(indexPath,html,'utf8');
console.log(`Dedicated C++ course prepared: ${lessons.length} lessons, ${lessons.reduce((n,l)=>n+l.examples.length,0)} examples, ${exercises.length} exercises, ${quiz.length} checks, ${projects.length+1} projects/capstone. Total source courses: ${courses.length}.`);
