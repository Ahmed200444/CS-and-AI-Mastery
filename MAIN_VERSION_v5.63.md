# CS & AI Mastery — v5.63

## Code-grounded, concise explanations

- Example descriptions now explain only behavior that is actually present in that example's code.
- Removed speculative wording such as saying code prints, returns, stores, uses I/O, or calls a function unless the corresponding code is present.
- `print()` by itself is explained as displaying a result; it no longer causes an unrelated I/O glossary entry.
- I/O, `io`, and `sys` glossary entries appear only when the code explicitly imports or uses those stream/module features.
- Common methods and operations such as `.strip()`, `.join()`, `.split()`, `.lower()`, `.upper()`, `.startswith()`, `.endswith()`, `.replace()`, `.count()`, `.get()`, and `.pop()` are explained when they actually occur.
- Main lesson explanations are capped to a shorter quick explanation, with an even simpler concise explanation available.
- The wording is beginner-friendly while deeper source material remains available without crowding the normal lesson view.
- Existing runners, progress, GitHub publishing, assessments, projects, and the v5.61 scroll architecture remain intact.
