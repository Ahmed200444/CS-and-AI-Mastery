# C++ Programming & DSA — final QA

- Course id: `cpp-dsa`
- 54 lessons
- 54 exercises
- 54 checkpoints/quizzes
- 6 portfolio projects
- 5–8 study examples per lesson through the shared adaptive-example layer
- Deep lesson explanation appears before the course-native example
- Line-by-line + syntax explanation layer recognizes C++ syntax
- C++ Run/Check uses the shared `CSAICppRunner`
- Fast C++ runtime is prepared/cached locally by the PC installer
- Project/example/exercise GitHub publishing supports `.cpp`
- Smart README uses a C++17 compile/run command
- Dedicated C++ contract test is part of `npm test`
- 54/54 native lesson examples passed `g++ -std=c++17 -Wall -Wextra -pedantic -fsyntax-only`
- Legacy primary-Python UI guard preserves the dedicated C++ course instead of rewriting it to Python
